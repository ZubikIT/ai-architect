# C4-диаграммы — корпоративная AI-платформа

> Собрано из ADR-пакета [`../docs/adr/`](../docs/adr/). Стек: on-prem / air-gapped, 2× H100 NVL, общий LLM Qwen3.6 (vLLM/FP8), две плоскости — Open WebUI (персонально) и Onyx (совместно), MCP как слой интеграции.
>
> Уровни: **C1 Context → C2 Container → C3 Component (Agent internals) → Deployment**. Sequence и ER — отдельно.

## C1 — System Context

```mermaid
flowchart TB
  emp["Сотрудник<br/>персональное использование"]
  team["Команды / коллеги<br/>совместная работа"]
  admin["Администратор / DevOps"]

  subgraph plat["Корпоративная AI-платформа · on-prem / air-gapped"]
    core["AI-платформа<br/>личный чат · поиск по общим БЗ · агенты"]
  end

  conf["Confluence"]
  jira["Jira"]
  sp["SharePoint"]
  idp["Корпоративный IdP<br/>SSO / OIDC"]
  intsys["Внутренние системы / API"]
  noext["Внешние LLM API<br/>OpenAI / Anthropic"]

  emp -->|"личный чат / агент · HTTPS"| core
  team -->|"поиск по общим БЗ · HTTPS"| core
  admin -->|"управление, RBAC"| core
  core -->|"коннекторы, permission-aware · в контуре"| conf
  core --> jira
  core --> sp
  core -->|"аутентификация"| idp
  core -->|"действия · MCP"| intsys
  core -. "❌ запрещено — санкции, № 99-З" .-> noext

  classDef forbidden fill:#fee,stroke:#c00,stroke-dasharray:4 3;
  class noext forbidden;
```

## C2 — Containers

```mermaid
flowchart TB
  emp["Сотрудник"]
  team["Команды"]
  mobile["Conduit<br/>моб. клиент"]
  conf["Confluence"]
  jira["Jira"]
  sp["SharePoint"]
  idp["IdP · SSO/OIDC"]
  intsys["Внутренние системы / API"]

  subgraph plat["AI-платформа · on-prem / air-gapped · 2× H100 NVL"]
    owui["Open WebUI<br/>персональный фронт · SSO, Pipelines<br/>ADR-0007"]
    onyx["Onyx<br/>совместный поиск по БЗ · коннекторы, permission-aware<br/>ADR-0008"]
    orch["Оркестрация агентов<br/>LangGraph · stateful, HITL<br/>ADR-0005"]
    vllm["LLM Serving<br/>vLLM + Qwen3.6-27B · FP8<br/>ADR-0002 / 0003 / 0006"]
    qdr[("Vector DB · Qdrant<br/>персон./агентный RAG · ADR-0004")]
    onyxidx[("Индекс Onyx<br/>общие БЗ")]
    mcp["MCP-серверы<br/>слой интеграции · ADR-0009"]
    state[("Состояние агентов<br/>Postgres / Redis")]
    vault["Secrets · Vault"]
  end

  emp --> owui
  mobile --> owui
  team --> onyx
  owui -->|"OpenAI API"| vllm
  onyx -->|"OpenAI API · общий LLM"| vllm
  owui --> orch
  orch -->|"OpenAI API"| vllm
  orch -->|"RAG: hybrid + rerank"| qdr
  orch -->|"инструменты"| mcp
  orch -->|"общая БЗ · MCP"| onyx
  owui -->|"MCP-клиент"| mcp
  onyx -->|"MCP клиент/сервер"| mcp
  onyx --> onyxidx
  onyx -->|"коннекторы · в контуре"| conf
  onyx --> jira
  onyx --> sp
  mcp -->|"действия"| intsys
  owui --> idp
  onyx --> idp
  orch --> state
  vllm -. секреты .- vault
```

## C3 — Components (Agent internals · LangGraph)

```mermaid
flowchart TB
  subgraph agent["Оркестрация агентов (LangGraph) · внутреннее устройство · ADR-0005"]
    sup["Supervisor / Менеджер<br/>приём задачи, делегирование, сборка"]
    planner["Planner<br/>ReAct / Plan-and-Execute"]
    workers["Worker-агенты<br/>retriever · analyst · action"]
    mem["Memory<br/>краткосрочная (контекст) + долгосрочная"]
    tools["Tools interface<br/>MCP-клиент"]
    retr["RAG Retriever<br/>hybrid + rerank"]
    guard["Guardrails<br/>PII · prompt-injection · лимиты циклов/бюджета"]
    llmc["LLM Client<br/>OpenAI API"]
    ckpt["Checkpointer<br/>persistence состояния"]
  end

  vllm["vLLM + Qwen3.6"]
  qdr[("Qdrant")]
  mcp["MCP-серверы"]
  onyx["Onyx · общая БЗ"]
  state[("Postgres / Redis")]

  sup --> planner
  planner --> workers
  workers --> guard
  guard --> llmc
  llmc --> vllm
  workers --> retr
  retr --> qdr
  workers --> tools
  tools --> mcp
  tools -->|"общая БЗ"| onyx
  sup --> mem
  sup --> ckpt
  ckpt --> state
```

## Deployment

```mermaid
flowchart TB
  user["Пользователи · корп. сеть"]

  subgraph dmz["DMZ"]
    proxy["Reverse proxy / TLS"]
    owuiD["Open WebUI"]
    onyxUI["Onyx UI"]
  end

  subgraph internal["Internal zone · без egress к внешним LLM"]
    gpu["GPU-нода · Lenovo SR675 V3<br/>2× H100 NVL 94GB<br/>vLLM + Qwen3.6 FP8"]
    qdrD[("Qdrant")]
    onyxB["Onyx backend + индекс"]
    lg["LangGraph workers"]
    mcpD["MCP-серверы"]
    pg[("Postgres · состояние агентов")]
    vaultD["Vault · секреты"]
  end

  subgraph conn["Connector egress · контролируемый, в контуре"]
    conf2["Confluence"]
    jira2["Jira"]
    sp2["SharePoint"]
  end

  user --> proxy
  proxy --> owuiD
  proxy --> onyxUI
  owuiD --> gpu
  owuiD --> lg
  onyxUI --> onyxB
  onyxB --> gpu
  lg --> gpu
  lg --> qdrD
  lg --> mcpD
  lg --> pg
  onyxB --> qdrD
  onyxB --> conf2
  onyxB --> jira2
  onyxB --> sp2
  gpu -. секреты .- vaultD
  onyxB -. секреты .- vaultD
```

## Соответствие ADR → диаграммы

| Решение | C1 | C2 | C3 | Deployment |
|---|:--:|:--:|:--:|:--:|
| ADR-0001 on-prem / air-gapped | ✅ (границы, ❌ внешние LLM) | ✅ | | ✅ (зоны, нет egress) |
| ADR-0002/0003/0006 Qwen3.6 · vLLM · FP8 | | ✅ LLM Serving | ✅ LLM Client | ✅ GPU-нода |
| ADR-0004 Qdrant | | ✅ | ✅ RAG Retriever | ✅ |
| ADR-0005 LangGraph | | ✅ оркестрация | ✅ (весь C3) | ✅ workers |
| ADR-0007 Open WebUI | ✅ (персон.) | ✅ | | ✅ DMZ |
| ADR-0008 Onyx | ✅ (совмест.) | ✅ + коннекторы | | ✅ DMZ + egress |
| ADR-0009 MCP | | ✅ MCP-серверы | ✅ Tools interface | ✅ |

> TODO (бриф): **Sequence** (User → Guardrails → Rerank → Agent Loop → Tools → Response) и **ER** (vectors, chunks, sessions, logs, RBAC).
