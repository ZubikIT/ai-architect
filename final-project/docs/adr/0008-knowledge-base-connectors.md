---
id: ADR-0008
title: Коннекторы к корпоративным БЗ — Onyx (ex-Danswer)
date: 2026-05-26
status: proposed
deciders: [Зубик Александр]
tags: [connectors, ingestion, onyx, danswer, confluence, jira, sharepoint, permission-aware, rag]
---

# ADR-0008. Коннекторы к корпоративным БЗ — Onyx (ex-Danswer)

## Контекст
База знаний живёт во внешних корпоративных системах: **Confluence, Jira, SharePoint** (потенциально + Slack/GitLab/почта). Нужен слой подключения и ingestion. Силы давления:

- **Permission-aware:** индекс должен **наследовать права источника** → пользователь в чате видит только то, к чему имеет доступ (RBAC, № 99-З). Это самая сложная часть — не просто «выкачать текст».
- **Инкрементальная синхронизация:** поддерживать актуальность (новые/изменённые страницы, тикеты), а не разовый дамп.
- **Air-gapped (ADR-0001):** коннекторы и индекс — в контуре, с локальными LLM, без интернета.
- **Уже выбрано:** Qdrant (ADR-0004), Open WebUI (ADR-0007), LangGraph (ADR-0005), hybrid+rerank (урок 06).
- **Команда:** начальный ML → строить 3+ коннектора с зеркалированием прав своими силами — дорого и рискованно.

## Рассмотренные варианты
1. **Onyx (ex-Danswer)** _(выбран как слой коннекторов)_
   - **Хорошо:** **MIT**, **40+ коннекторов** (Confluence/Jira/SharePoint/Slack/Drive/GitHub/…) с **permission-aware retrieval**; self-host (Docker/K8s/Helm); **полностью air-gapped с локальными LLM**; hybrid BM25+vector + cross-encoder reranking «из коробки» (совпадает с уроком 06); поддержка нескольких vector DB.
   - **Плохо:** это **полная платформа** (свой UI, свой RAG, свой индекс) → **пересекается с ADR-0004 (Qdrant) и ADR-0007 (Open WebUI)**; тяжёлый ops-footprint.
2. **Кастомные коннекторы** (Atlassian REST / MS Graph API → парсинг → Qdrant)
   - **Хорошо:** максимум контроля, точно наш стек (пишем прямо в Qdrant из ADR-0004).
   - **Плохо:** огромный объём — инкрементальная синхра, **зеркалирование прав**, rate limits, форматы; переизобретаем то, что Onyx уже умеет. Не для команды с начальным ML.
3. **LlamaIndex / LangChain readers + собственный слой прав**
   - **Хорошо:** готовые ридеры для источников.
   - **Плохо:** **permission-aware** и инкрементальность — всё равно на нас; по трудозатратам близко к кастому.
4. **Коммерческие (Glean и т.п.)** — **Rejected:** SaaS/проприетарно, санкции, не air-gapped.

## Решение
Берём **Onyx** как **слой коннекторов + permission-aware ingestion** (его сильнейшая сторона), а не как замену всего стека. Целевая роль — **knowledge backend**:

- Onyx подключает и инкрементально индексирует Confluence/Jira/SharePoint **с наследованием прав**.
- Агенты (LangGraph, ADR-0005) и/или Open WebUI (ADR-0007) обращаются к Onyx через его **search/answer API** (Onyx как retriever/инструмент).
- Open WebUI остаётся пользовательским фронтом; UI Onyx можно включить опционально как enterprise-search.

**Статус `proposed`** — до решения нужно закрыть **открытые вопросы**:
1. **Полная платформа vs только коннекторы.** Если возьмём Onyx целиком (его UI+RAG), то **ADR-0007 (Open WebUI) и часть кастомного RAG → `superseded`**. Решить явно.
2. **Vector store:** поддерживает ли Onyx **Qdrant** как backend? Если только встроенный (Vespa) — конфликт с **ADR-0004**: либо Qdrant под агентный RAG + индекс Onyx под enterprise-search (две БД), либо перейти на индекс Onyx (пересмотр ADR-0004).
3. **CE (MIT) vs EE:** какие нужные фичи (тонкий RBAC, SSO, коннекторы) только в Enterprise Edition.

## Последствия
- **Положительные:**
  - Готовые **permission-aware** коннекторы → огромная экономия против кастома; закрывает RBAC/№ 99-З на уровне retrieval.
  - Air-gapped и MIT — соответствует ADR-0001 без лицензионных рисков.
  - Hybrid+rerank из коробки → меньше собственного RAG-кода (урок 06).
- **Отрицательные / риски:**
  - **Дублирование** Vector DB / UI / RAG с ADR-0004/0007 → требует явного решения роли (иначе два конкурирующих стека).
  - Тяжёлая платформа (ops); сопровождение и обновления коннекторов.
- **Что придётся изменить дальше:**
  - **PoC:** Onyx + один коннектор (Confluence) в air-gapped; проверить Qdrant-backend и зеркалирование прав.
  - По итогам PoC — зафиксировать роль Onyx и при необходимости обновить ADR-0004/0007 (`superseded by`).

## Compliance & Ethics _(AI-специфичный раздел, урок 08)_
- **Permission-aware retrieval** — ключ к № 99-З: пользователь не получает в ответе чужие документы; права наследуются из источника.
- **Air-gapped:** все коннекторы работают внутри контура; для облачных SharePoint/Atlassian — через разрешённый внутренний прокси/коннектор, без выноса данных наружу.
- Ретеншн/удаление: синхронизировать удаление в источнике с удалением из индекса.

## Связи
- **Следует из:** [ADR-0001](0001-on-premise-self-hosted-llm.md) (air-gapped).
- **Конфликтует / требует согласования с:** [ADR-0004](0004-vector-db.md) (vector store), [ADR-0007](0007-chat-interface.md) (UI/RAG overlap).
- **Связан с:** [ADR-0005](0005-orchestration.md) (Onyx как retriever-tool для агентов), урок 06 (hybrid + reranking — Onyx делает из коробки).
- **Шаблон:** [`../../../templates/adr.md`](../../../templates/adr.md).
