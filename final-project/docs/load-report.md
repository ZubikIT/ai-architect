# Нагрузочный отчёт

> Сдаточный артефакт по [ТЗ занятия 32](../sessions/32-vybor-temy.md): «RPS и латентность на своём железе». Методика — [ДЗ-24](../../24-high-load-low-latency/Zubik_DZ-24_highload-realtime.md), детали и допущения вынесены в [`load/methodology.md`](load/methodology.md), сырые замеры — [`load/raw.json`](load/raw.json).
>
> **Дата прогона:** 12.09.2026 · **Конфигурация:** коммит стека на момент прогона, `docker compose --profile core --profile obs`.

## Резюме

Три вывода, ради которых отчёт делался:

1. **Узкое место — cross-encoder rerank: 76–90 % времени ответа.** Не граф, не векторный поиск, не LLM. Обход графа стоит **~1 %**, поиск в Qdrant — **0,3 %**.
2. **Пропускная способность не растёт с параллелизмом вообще.** 1,5 → 1,9 RPS на ступенях от 1 до 16 клиентов, при этом латентность растёт линейно (×12,7). Один запрос уже занимает все ядра — дальнейший параллелизм только выстраивает очередь. Масштабирование здесь **только горизонтальное**, и это ровно то, что заложено в [ADR-0006](adr/0006-kvantovanie-i-sizing-gpu.md) и [ДЗ-24](../../24-high-load-low-latency/Zubik_DZ-24_highload-realtime.md).
3. **Мультиагентная топология почти ничего не стоит сама по себе.** Накладные расходы супервизора и сведения — **0,8 %** (15 мс из 1981 мс). Цена MAS — это веер retrieval, а не оркестрация: два агента ≈ два поиска.

Отдельный результат, который меняет модель ёмкости: **широкие права дороже узких**. ACL-предикат работает как pre-filter, и чем больше документов доступно субъекту, тем больше кандидатов доходит до реранка, стоимость которого линейна по их числу.

## Стенд

| | |
|---|---|
| Хост | QEMU Virtual CPU, 8 vCPU, 31 ГБ RAM, Linux 6.8.0 |
| GPU | у стенда нет; генерация замерена отдельно — на **боевом инференсе платформы** (см. раздел ниже) |
| Сервис | контейнер `cifrovye-sotrudniki-backend` (3,31 ГБ), **один воркер uvicorn**, без лимитов CPU и памяти |
| Данные | Neo4j 5 Community, Qdrant, PostgreSQL — в контейнерах на том же хосте |
| Корпус | 4 документа ЛПА, 19 чанков |
| Модели | эмбеддер `paraphrase-multilingual-MiniLM-L12-v2` (384d), реранкер `ms-marco-MiniLM-L-6-v2` — оба на CPU |
| Генератор нагрузки | [`tools/loadtest.py`](../backend/tools/loadtest.py), **на том же хосте** |

Абсолютные цифры к прод-конфигурации не переносятся. Переносится то, ради чего замер и делался: **соотношение стоимости шагов** и **причина насыщения**.

## Результаты

30 с на ступень, 5 с прогрева отбрасывается. Ошибок — **0 %** во всех прогонах, включая ступени с латентностью в десятки секунд: сервис деградирует по времени, но не по отказам.

> **О размере выборки.** Ступень длится фиксированное время, поэтому чем медленнее сценарий, тем меньше замеров: от **18** запросов (`agents`, 1 клиент) до **64** (`ask`, 16 клиентов). Для p50 этого достаточно, для p99 — нет: на 18 замерах p99 вырождается в максимум, что видно в таблице `agents` (p95 и p99 совпали). Поэтому все выводы ниже опираются на **p50 и p95**, а p99 приводится как есть и как основание ни для чего не служит.

### `GET /healthz` — база отсчёта

| Клиентов | RPS | p50 | p95 | p99 |
|---:|---:|---:|---:|---:|
| 1 | 421,9 | 2,3 мс | 2,9 мс | 3,2 мс |
| 2 | 501,9 | 3,4 мс | 8,0 мс | 10,0 мс |
| 4 | **671,8** | 5,5 мс | 9,0 мс | 10,4 мс |
| 8 | 554,3 | 9,9 мс | 37,2 мс | 74,8 мс |
| 16 | 431,0 | 19,6 мс | 115,1 мс | 183,6 мс |

Обвязка (ASGI, middleware, экспорт метрик) стоит **2,3 мс** — это 0,35 % ответа `/ask`. Максимум на 4 клиентах и спад дальше — генератор и сервис делят те же 8 ядер; это ограничение стенда, а не сервиса.

### `POST /ask` — одиночный GraphRAG

| Клиентов | RPS | p50 | p95 | p99 | замеров |
|---:|---:|---:|---:|---:|---:|
| 1 | 1,53 | 651 мс | 720 мс | 788 мс | 46 |
| 2 | 1,65 | 1 205 мс | 1 329 мс | 1 354 мс | 50 |
| 4 | 1,75 | 2 261 мс | 2 524 мс | 2 607 мс | 54 |
| 8 | 1,88 | 4 226 мс | 4 538 мс | 4 621 мс | 61 |
| 16 | 1,88 | 8 290 мс | 9 156 мс | 9 264 мс | 64 |

### `POST /ask` с ролью `legal` — тот же путь, больше доступных документов

| Клиентов | RPS | p50 | p95 | p99 |
|---:|---:|---:|---:|---:|
| 1 | 0,99 | 1 001 мс | 1 112 мс | 1 174 мс |
| 4 | 1,13 | 3 539 мс | 3 705 мс | 3 729 мс |
| 16 | 1,17 | 13 580 мс | 14 039 мс | 14 078 мс |

**Тот же запрос с более широкими правами стоит в 1,5 раза дороже** (1 001 мс против 651 мс). Причина не в графе и не в проверке прав самой по себе — ниже.

### `POST /agents/ask` — мультиагентный путь (веер из двух ролей)

| Клиентов | RPS | p50 | p95 | p99 |
|---:|---:|---:|---:|---:|
| 1 | 0,58 | 1 715 мс | 1 911 мс | 1 911 мс |
| 4 | 0,68 | 5 925 мс | 6 055 мс | 6 140 мс |
| 16 | 0,70 | 22 166 мс | 23 902 мс | 23 983 мс |

## Где именно уходит время

Разложение по спанам OpenTelemetry ([ADR-0017](adr/0017-observability.md)), один запрос без конкуренции:

| Шаг | роль `all` | роль `legal` | Доля |
|---|---:|---:|---:|
| `guardrail.input` | 0,0 мс | 0,0 мс | 0 % |
| `retrieve.embed` | 188 мс | 38 мс | 3–22 % |
| `retrieve.dense` (Qdrant) | 2,8 мс | 3,0 мс | **0,3 %** |
| `retrieve.bm25` | 0,2 мс | 0,3 мс | 0 % |
| **`retrieve.rerank`** | **664 мс** | **1 036 мс** | **76–90 %** |
| `graph.expand` (Neo4j) | 8,7 мс | 4,7 мс | ~0,5 % |
| `graph.annotate` | 2,3 мс | 7,1 мс | ~0,5 % |
| `guardrail.output` | 0,1 мс | 0,1 мс | 0 % |
| **Итого** | **868 мс** | **1 154 мс** | |

Реранк получил **14** кандидатов в первом случае и **19** во втором — 47 и 55 мс на кандидата. Отсюда и разница между ролями: ACL-предикат работает как pre-filter, роль `legal` открывает ЛПА-03, кандидатов становится больше, и каждый из них проходит через cross-encoder.

**Граф — не узкое место.** Вопрос «не тормозит ли GraphRAG», который логично задать на защите, закрывается замером: обход и аннотации вместе — 11–12 мс, около 1 % ответа. Отказываться от графа ради латентности нечего.

### Цена мультиагентной топологии

| Составляющая | Время | Доля |
|---|---:|---:|
| `agent.hr` (права `all`, 7 пунктов) | 800,6 мс | 40,4 % |
| `agent.compliance` (права `legal`, 8 пунктов) | 1 165,3 мс | 58,8 % |
| `supervisor.route` | 0,1 мс | 0,005 % |
| `supervisor.synthesize` | 0,4 мс | 0,02 % |
| Прочие накладные расходы | 15 мс | 0,8 % |
| **Итого `agents.ask`** | **1 981 мс** | 100 % |

Маршрутизация стоит **0,1 мс**, потому что она детерминирована правилами и не ходит в LLM ([ADR-0015](adr/0015-topologiya-mas-cifrovye-sotrudniki.md), правило 1). Сведение — 0,4 мс, потому что при одном непустом выводе супервизор не делает лишнего вызова модели.

Это прямой ответ на открытый вопрос ADR-0015 («цена топологии обязана окупаться»): **оркестрация бесплатна, платим только за веер**. Значит, экономить нужно на точности маршрутизации — каждый лишний адресат удваивает стоимость запроса, а не на самой иерархии.

## Почему параллелизм не даёт пропускной способности

`RPS` на `/ask` растёт с 1,53 до 1,88 (+23 %), пока латентность растёт с 651 до 8 290 мс (+1 173 %). По закону Литтла `N = RPS × latency`: 16 × ≈ 1,88 × 8,29 — сходится, то есть очередь чисто накопительная.

Это подпись **полностью сериализованного ресурса, насыщенного уже на одном клиенте**. Причина конкретна: torch распараллеливает эмбеддинг и реранк внутри одного вызова по всем ядрам. Один запрос уже занимает машину целиком; второй ждёт.

Практическое следствие: **увеличивать число потоков или воркеров на одном хосте бессмысленно** — они будут драться за те же ядра. Рост только горизонтальный, репликами с выделенными ядрами, и это совпадает с тем, что заложено в архитектуре ([ДЗ-24](../../24-high-load-low-latency/Zubik_DZ-24_highload-realtime.md), три независимых пула; [ADR-0006](adr/0006-kvantovanie-i-sizing-gpu.md)).

## Проверка SLO

| Источник | Цель | Максимум клиентов на реплику |
|---|---|---:|
| [NFR](requirements.md#нефункциональные-требования-nfr) | p95 ≤ 5 с (короткий ответ) | **8** (4 538 мс) |
| NFR | p95 ≤ 15 с (агентный) | **8** (11 755 мс) |
| [ADR-0017](adr/0017-observability.md) | p95 ≤ 3 с без агентного цикла | **4** (2 524 мс) |
| ADR-0017 | p95 ≤ 8 с с циклом | **4** (6 055 мс) |

По более строгой редакции SLO одна реплика на этом стенде держит **4 одновременных клиента** при 1,75 RPS (одиночный путь) и 0,68 RPS (агентный). В пересчёте на людей: при одном вопросе в минуту — порядка **100 сотрудников на реплику**.

**И это без генерации.** С подключённым vLLM к каждому ответу добавится prefill и декодирование; ёмкость будет ниже. Цифры выше — верхняя граница, а не прогноз.

## Что из этого следует

Отсортировано по отношению эффекта к затратам:

1. **Реранк — на ONNX и в отдельный пул.** 76–90 % латентности в одном компоненте. Именно это и заложено в бюджете [ДЗ-24](../../24-high-load-low-latency/Zubik_DZ-24_highload-realtime.md) («rerank только top-20, ONNX на CPU-пуле», 35 мс) — замер показывает, что без этого бюджет недостижим на порядок, а не на проценты.
2. **Ограничить число кандидатов реранка.** Стоимость линейна: `SUFLER_TOP_K` с 20 до 10 срезает около 0,5 с с запроса. Цена — возможная потеря релевантности, и её надо мерить на golden set, а не решать в отчёте.
3. **Кэш (L1 exact + L2 semantic) из ДЗ-24 в MVP отсутствует** — и замер показывает, чего это стоит: при 60 % повторяющихся вопросов кэш убирает весь путь в 0,65–1,15 с для большинства запросов. Это самая дешёвая оптимизация из возможных, потому что не требует трогать модели.
4. **Учитывать широту прав в планировании ёмкости.** Привилегированный пользователь дороже обычного в 1,5 раза на том же вопросе. Rate limiting по числу запросов это не отражает.
5. **Проставить лимиты CPU контейнерам.** Сейчас их нет: при нескольких репликах на хосте они будут конкурировать непредсказуемо, и ни один замер не будет воспроизводим.
6. **Граф не трогать.** 1 % латентности — оптимизировать нечего.

## Замер на живой модели платформы

Основная часть отчёта снята **без генерации**. После подключения компонентов к
боевому инференсу платформы (собственный GPU-бокс, MoE-модель в AWQ) картина
замерена целиком.

> **Нагрузку намеренно не поднимали выше двух клиентов.** Инференс обслуживает
> продуктивный трафик платформы; полная лестница параллелизма против прода — это
> не замер, а инцидент. Поэтому здесь ступени 1 и 2, а выводы о насыщении
> по-прежнему берутся из части без генерации.

| Сценарий | Клиентов | RPS | p50 | p95 |
|---|---:|---:|---:|---:|
| `ask` | 1 | 0,51 | 1 939 мс | 2 186 мс |
| `ask` | 2 | 0,70 | 2 824 мс | 3 363 мс |
| `agents` | 1 | 0,14 | 7 124 мс | 8 775 мс |
| `agents` | 2 | 0,21 | 9 626 мс | 10 331 мс |

### Где теперь уходит время

| Шаг | Время | Доля |
|---|---:|---:|
| **`llm.generate`** (своя модель в контуре) | **1 611 мс** | **68 %** |
| `retrieve.rerank` | 708 мс | 30 % |
| `retrieve.embed` | 36 мс | 1,5 % |
| `graph.expand` + `annotate` | 5,5 мс | **0,3 %** |
| `retrieve.dense` | 4,5 мс | 0,2 % |
| **Итого** | **2 368 мс** | |

Три вывода, которых не было в замере без генерации:

1. **Генерация дороже всего retrieval**, но не подавляюще: 68 % против 32 %. Это
   означает, что оптимизация реранка ([п. 1 рекомендаций](#что-из-этого-следует))
   остаётся осмысленной даже с подключённой моделью — она снимает треть ответа.
2. **Граф остался в пределах 0,3 %** — теперь уже в полном бюджете ответа.
   Вопрос «не тормозит ли GraphRAG» закрыт окончательно.
3. **Характер узкого места меняется с параллелизмом.** Retrieval выполняется на
   CPU этой машины и от параллелизма не выигрывает вовсе; генерация идёт на
   отдельном движке с батчингом и выигрывает: RPS растёт 0,51 → 0,70 при переходе
   от одного клиента к двум, чего на чистом retrieval не происходило.

### Мультиагентный путь с генерацией дорожает непропорционально

Без генерации веер из двух ролей стоил ×2,6 одиночного пути. С генерацией —
**×3,7** (7 124 мс против 1 939 мс), потому что к двум проходам retrieval
добавляются **три** вызова модели: по одному на каждого «сотрудника» и один на
сведение.

Это уточняет вывод из [отчёта по golden set](eval-report.md): топология не дала
прироста качества и при этом дорожает быстрее, чем растёт число агентов. Аргумент
в пользу жёсткой маршрутизации без веера там, где уверенность высока.

### Проверка SLO с генерацией

| Источник | Цель | Держит |
|---|---|---:|
| [NFR](requirements.md) | p95 ≤ 5 с (короткий ответ) | **2 клиента** (3 363 мс) |
| NFR | p95 ≤ 15 с (агентный) | **2 клиента** (10 331 мс) |
| [ADR-0017](adr/0017-observability.md) | p95 ≤ 3 с без цикла | **1 клиент** (2 186 мс) |
| ADR-0017 | p95 ≤ 8 с с циклом | **не держит уже на 1** (8 775 мс) |

Строгий SLO агентного пути нарушается на первом же клиенте. Это не повод менять
цифру в ADR: это повод либо убрать лишний вызов модели на сведении при одном
непустом выводе, либо признать, что 8 секунд для агентного сценария недостижимы
на текущем железе, и пересмотреть цель осознанно.

## Чего в этом отчёте нет

Названо прямо, чтобы не выдавать частичный замер за полный:

- **Поведение генерации под нагрузкой** (TTFT, токены/с, continuous batching при высоком параллелизме) — не мерилось: инференс продуктивный, полная лестница против него не запускалась. Оценка ёмкости движка — [ADR-0006](adr/0006-kvantovanie-i-sizing-gpu.md) и [ДЗ-17](../../17-optimizaciya-inferensa-llm/Zubik_DZ-17_sizing-inference.md).
- **Открытый профиль нагрузки** с фиксированным arrival rate и backpressure — здесь закрытая петля, она отвечает на вопрос о насыщении, но не даёт латентность «при X RPS».
- **Смешанный профиль** классов A/B/C из ДЗ-24 и поведение кэша — кэша в MVP нет.
- **Прод-объём индекса** (10⁷ векторов): на демо-корпусе из 19 чанков векторный поиск заведомо дешевле реального. Впрочем, на выводы это не влияет — узкое место не в поиске, а в реранке, стоимость которого от размера индекса не зависит.
- **Длительная нагрузка** (soak): утечки памяти и деградация на часах не проверялись.

---

**Воспроизведение, сырые данные и допущения** — [`load/methodology.md`](load/methodology.md).
