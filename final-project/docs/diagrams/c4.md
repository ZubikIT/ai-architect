# C4-диаграммы — мультиагентная платформа цифровых корпоративных сотрудников

> Собрано из ADR-пакета [`../docs/adr/`](../adr). Стек: on-prem / air-gapped, 2× H100 NVL, общий LLM Qwen3.5-27B (vLLM/FP8), знания — гибрид **Qdrant + Neo4j** (GraphRAG), агенты — иерархический MAS на LangGraph.
>
> Уровни: **C1 Context → C2 Container → C3 Component (Agent internals) → Deployment**. [Data Flow](data-flow.md) и [Sequence/ER](sequence-er.md) — отдельно.
>
> Сквозной принцип, требуемый ТЗ: **Control Plane** (агенты, политики, оркестрация) отделён от **Data Plane** (хранилища знаний и модели). Агенты не имеют драйверов БД — они вызывают инструменты слоя репозитория ([ADR-0015](../adr/0015-topologiya-mas-cifrovye-sotrudniki.md), [ADR-0016](../adr/0016-acl-na-uzlah-grafa.md)).

## C1 — System Context

```mermaid
flowchart TB
  emp["Сотрудник<br/>вопрос по ЛПА, регламентам, техдокументации"]
  op["Оператор КЦ<br/>подсказка в реальном времени"]
  admin["Администратор / DevOps"]
  sec["Служба ИБ<br/>аудит доступа и red teaming"]

  subgraph plat["Платформа цифровых корпоративных сотрудников · on-prem / air-gapped"]
    core["AI-платформа<br/>агенты-роли · граф знаний · GraphRAG"]
  end

  sp["SharePoint · ЛПА<br/>PDF · DOCX · сканы"]
  conf["Confluence"]
  jira["Jira"]
  cad["Чертежи и схемы<br/>техдокументация"]
  idp["Keycloak · SSO / OIDC<br/>источник ролей"]
  intsys["Внутренние системы / API"]
  noext["Внешние LLM API<br/>OpenAI / Anthropic"]

  emp -->|"вопрос · HTTPS"| core
  op -->|"копайлот · HTTPS"| core
  admin -->|"эксплуатация, конфигурация ролей"| core
  core -->|"журнал выдач и отказов"| sec
  core -->|"коннекторы, permission-aware · в контуре"| sp
  core --> conf
  core --> jira
  core -->|"мультимодальный разбор"| cad
  core -->|"аутентификация, группы"| idp
  core -->|"действия · MCP · read-only по умолчанию"| intsys
  core -. "❌ запрещено — санкции, № 99-З, ADR-0001" .-> noext

  classDef forbidden fill:#fee,stroke:#c00,stroke-dasharray:4 3;
  class noext forbidden;
```

## C2 — Containers

```mermaid
flowchart TB
  emp["Сотрудник · оператор КЦ"]
  mobile["Conduit<br/>моб. клиент"]
  idp["Keycloak · SSO/OIDC"]
  src["Источники документов<br/>SharePoint · Confluence · Jira · чертежи"]
  intsys["Внутренние системы / API"]

  subgraph plat["Платформа · on-prem / air-gapped · 2× H100 NVL"]
    subgraph cp["Control Plane · агенты и политики"]
      owui["Open WebUI<br/>фронт · SSO · Pipelines<br/>ADR-0007 / 0011"]
      api["Backend API<br/>FastAPI · SSE-стриминг<br/>валидация JWT · RequestContext"]
      orch["Оркестрация MAS<br/>LangGraph · supervisor + роли-агенты<br/>ADR-0015"]
      repo["Слой репозитория<br/>единственная точка сборки запросов<br/>ACL-предикат обязателен · ADR-0016"]
      guard["Guardrails<br/>PII · prompt injection · лимиты"]
      mcp["MCP-серверы<br/>инструменты · ADR-0009"]
      ing["Конвейер индексации<br/>layout-парсер → VL на исключениях<br/>ADR-0014"]
    end

    subgraph dp["Data Plane · знания и модели"]
      vllm["LLM Serving<br/>vLLM + Qwen3.5-27B · FP8<br/>ADR-0002 / 0003 / 0006"]
      vlm["VL-разбор · батч<br/>Qwen3-VL + адаптер Zubr<br/>вне пиковых часов"]
      qdr[("Qdrant<br/>dense + BM25 · payload acl_roles<br/>ADR-0004")]
      neo[("Neo4j<br/>граф знаний · ADR-0012")]
      pg[("PostgreSQL<br/>checkpointer · сессии · аудит")]
    end

    subgraph obs["Observability · ADR-0017"]
      otel["OpenTelemetry Collector"]
      jae["Jaeger · трейсы"]
      lf["Langfuse · LLM-трейсы и скоры"]
      graf["Grafana + метрики<br/>Prometheus-совместимое хранилище"]
    end

    vault["Secrets · Vault"]
  end

  emp --> owui
  mobile --> owui
  owui --> api
  api -->|"роли из проверенного JWT"| idp
  api --> guard --> orch
  orch --> repo
  repo -->|"hybrid + payload-фильтр"| qdr
  repo -->|"шаблоны Cypher · обход 1-2 хопа"| neo
  orch -->|"OpenAI API"| vllm
  orch -->|"инструменты"| mcp --> intsys
  orch --> pg
  src --> ing
  ing --> vlm
  ing --> qdr
  ing --> neo
  ing -->|"права источника → acl_roles"| idp
  api -.->|"спаны, метрики"| otel
  orch -.->|"шаги агента, промпты"| otel
  vllm -.->|"vllm:* · nvidia_smi_*"| otel
  otel --> jae
  otel --> lf
  otel --> graf
  vllm -. секреты .- vault
  ing -. секреты .- vault
```

## C3 — Components (Agent internals · LangGraph)

```mermaid
flowchart TB
  subgraph agent["Control Plane · внутреннее устройство агентного слоя · ADR-0015"]
    ctx["RequestContext<br/>subject · roles · request_id<br/>создаётся один раз, неизменяем"]
    sup["Supervisor<br/>маршрутизация правилами,<br/>LLM-классификация как fallback"]
    subgraph roles["Цифровые сотрудники · роли"]
      r1["Юрист по ЛПА"]
      r2["Кадровик"]
      r3["Инженер по техдокументации"]
      r4["Комплаенс-контролёр"]
    end
    planner["Planner<br/>ReAct · видимый trace<br/>Thought → Action → Observation"]
    mem["Memory<br/>краткосрочная (контекст диалога)<br/>+ shared dossier по request_id"]
    lim["Лимиты<br/>шаги · вызовы · бюджет токенов"]
    tools["Tools interface<br/>MCP-клиент · read-only по умолчанию"]
    repo[["Repository layer<br/>GraphRAG-поиск · шаблоны Cypher<br/>ACL-предикат на каждом узле"]]
    rr["Rerank · cross-encoder"]
    guard["Guardrails IN / OUT<br/>PII · injection · цитаты · «не знаю»"]
    llmc["LLM Client<br/>OpenAI API"]
    ckpt["Checkpointer<br/>persistence состояния"]
  end

  vllm["vLLM + Qwen3.5-27B"]
  qdr[("Qdrant")]
  neo[("Neo4j")]
  mcpx["MCP-серверы"]
  pgx[("PostgreSQL")]
  otel["OTel · спаны каждого шага"]

  ctx --> sup
  sup --> r1
  sup --> r2
  sup --> r3
  sup --> r4
  r1 --> planner
  r2 --> planner
  r3 --> planner
  r4 --> planner
  planner --> lim
  planner --> guard
  guard --> llmc --> vllm
  planner --> repo
  ctx -.->|"roles · обязательный аргумент"| repo
  repo --> qdr
  repo --> neo
  repo --> rr --> planner
  planner --> tools --> mcpx
  sup --> mem
  sup --> ckpt --> pgx
  planner -.-> otel
  repo -.->|"выдача и отказ"| pgx
```

## Deployment

```mermaid
flowchart TB
  user["Пользователи · корп. сеть"]

  subgraph dmz["DMZ"]
    proxy["Reverse proxy / TLS"]
    owuiD["Open WebUI"]
    kc["Keycloak"]
  end

  subgraph internalCP["Internal · Control Plane · без egress к внешним LLM"]
    apiD["Backend API + LangGraph workers"]
    mcpD["MCP-серверы · sandbox, least privilege"]
    ingD["Indexing worker · очередь"]
  end

  subgraph internalDP["Internal · Data Plane"]
    gpu["GPU-нода · Lenovo SR675 V3<br/>2× H100 NVL 94GB<br/>vLLM + Qwen3.5 FP8<br/>батч VL в окне"]
    qdrD[("Qdrant")]
    neoD[("Neo4j · дампы по расписанию")]
    pgD[("PostgreSQL · состояние, аудит")]
  end

  subgraph obsD["Observability"]
    otelD["OTel Collector"]
    jaeD["Jaeger"]
    lfD["Langfuse"]
    grafD["Grafana + метрики"]
    tg["Alerting → Telegram"]
  end

  subgraph conn["Connector egress · контролируемый, в контуре"]
    sp2["SharePoint"]
    conf2["Confluence"]
    jira2["Jira"]
  end

  vaultD["Vault · секреты"]

  user --> proxy --> owuiD --> apiD
  proxy --> kc
  apiD -->|"JWKS"| kc
  apiD --> gpu
  apiD --> qdrD
  apiD --> neoD
  apiD --> pgD
  apiD --> mcpD
  ingD --> sp2
  ingD --> conf2
  ingD --> jira2
  ingD --> gpu
  ingD --> qdrD
  ingD --> neoD
  apiD -.-> otelD
  gpu -.-> otelD
  otelD --> jaeD
  otelD --> lfD
  otelD --> grafD --> tg
  gpu -. секреты .- vaultD
  ingD -. секреты .- vaultD
  apiD -. секреты .- vaultD
```

**Сетевые и эксплуатационные инварианты:**

- Из зоны Internal нет egress к внешним LLM-API ([ADR-0001](../adr/0001-on-premise-self-hosted-llm.md)); коннекторы к источникам — единственный контролируемый выход, внутри контура.
- Data Plane не принимает запросы напрямую от DMZ: путь всегда через Backend API, где валидируется JWT и создаётся `RequestContext`.
- Учётки БД разделены: индексация пишет, runtime читает ([ADR-0016](../adr/0016-acl-na-uzlah-grafa.md)).
- MCP-серверы — в песочнице, мутирующие операции только с подтверждением оператора ([ADR-0015](../adr/0015-topologiya-mas-cifrovye-sotrudniki.md)).
- Neo4j Community — одиночный инстанс; восстановление через дампы и переиндексацию, граф остаётся производным артефактом ([ADR-0012](../adr/0012-graph-db.md)).
- Батч VL-разбора делит GPU с онлайн-инференсом → запускается в окна низкой нагрузки ([ADR-0014](../adr/0014-multimodalnyy-ingestion.md)).

## Соответствие ADR → диаграммы

| Решение | C1 | C2 | C3 | Deployment | Data Flow |
|---|:--:|:--:|:--:|:--:|:--:|
| ADR-0001 on-prem / air-gapped | ✅ границы, ❌ внешние LLM | ✅ | | ✅ зоны, нет egress | ✅ |
| ADR-0002/0003/0006 Qwen3.5 · vLLM · FP8 | | ✅ LLM Serving | ✅ LLM Client | ✅ GPU-нода | ✅ |
| ADR-0004 Qdrant | | ✅ Data Plane | ✅ Repository | ✅ | ✅ |
| ADR-0005/0015 LangGraph · MAS | | ✅ оркестрация | ✅ весь C3 | ✅ workers | ✅ |
| ADR-0007/0011 Open WebUI · Pipe | ✅ канал | ✅ | | ✅ DMZ | ✅ |
| ADR-0008 Onyx-коннекторы | ✅ источники | ✅ индексация | | ✅ egress | ✅ |
| ADR-0009 MCP | | ✅ MCP-серверы | ✅ Tools interface | ✅ sandbox | |
| **ADR-0012 Neo4j** | | ✅ Data Plane | ✅ Repository | ✅ дампы | ✅ |
| **ADR-0013 GraphRAG** | | ✅ путь repo → qdr/neo | ✅ обход + rerank | | ✅ |
| **ADR-0014 мультимодальный ingestion** | ✅ чертежи, сканы | ✅ конвейер + VL | | ✅ окно батча | ✅ ступени |
| **ADR-0016 ACL** | ✅ служба ИБ | ✅ JWT → RequestContext | ✅ предикат в Repository | ✅ учётки БД | ✅ границы |
| **ADR-0017 Observability** | | ✅ OTel → Jaeger/Langfuse/Grafana | ✅ спаны шагов | ✅ + алерты | ✅ |
| **ADR-0018 security testing** | ✅ red teaming ИБ | | | | ✅ проверки на границах |

> **Data Flow** — в [`data-flow.md`](data-flow.md), **Sequence** и **ER** — в [`sequence-er.md`](sequence-er.md).
