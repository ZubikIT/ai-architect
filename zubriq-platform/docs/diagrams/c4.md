# Диаграммы C4 — как есть

Нарисовано по фактической конфигурации, а не по замыслу. Внутренняя адресация опущена сознательно ([почему](../../README.md)).

## C1 — Контекст

```mermaid
flowchart TB
  subgraph users["Пользователи"]
    org["Клиент-организация<br/>сотрудники, ключи, бюджеты"]
    dev["Разработчик<br/>OpenAI-совместимый ключ"]
    priv["Частный пользователь<br/>кошелёк"]
    staff["Персонал платформы<br/>админ-панель"]
  end

  ZQ["<b>ZubrIQ</b><br/>платформа доступа к языковым моделям<br/>юрисдикция РБ · расчёты в BYN"]

  subgraph external["Внешние системы"]
    agg["Агрегатор проприетарных моделей<br/>единый upstream-аккаунт"]
    vendors["Прямые вендоры моделей<br/>отдельные аккаунты"]
    dns["DNS и выпуск сертификатов"]
    play["Google Play<br/>дистрибуция мобильного чата"]
    pay["Приём оплат<br/>счета юрлицам в BYN"]
  end

  org --> ZQ
  dev --> ZQ
  priv --> ZQ
  staff --> ZQ

  ZQ -->|"часть запросов уходит наружу<br/>гейтится правами ключа"| agg
  ZQ --> vendors
  ZQ --- dns
  ZQ --- play
  ZQ --- pay

  ZQ -.->|"свои модели — внутри контура,<br/>наружу не уходят"| ZQ
```

Главное на этом уровне: **часть трафика по замыслу покидает контур**. Это не недосмотр, а второй режим доступа, который продаётся отдельно; вопрос лишь в том, кому он разрешён — см. [безопасность](../security.md).

## C2 — Контейнеры

```mermaid
flowchart TB
  user(["Пользователь / клиентское приложение"])

  subgraph edge["Периметр"]
    npm["Обратный прокси<br/>TLS, маршрутизация по именам<br/>wildcard-сертификат"]
  end

  subgraph products["Продукты"]
    chat["Чат<br/>React + Vercel AI SDK<br/>стриминг SSE"]
    console["Консоль клиента<br/>Laravel + Filament<br/><b>здесь же API чата</b>"]
    docs["Документация"]
    drive["Хранилище документов"]
    academy["Академия"]
    mcp["MCP-шлюз<br/>инструменты для агентов"]
    owui["Открытый чат-интерфейс<br/><i>legacy, до паритета нового</i>"]
  end

  subgraph platform["Платформенный слой"]
    gw["<b>Шлюз моделей</b><br/>ключи · бюджеты · учёт потребления<br/>маршрутизация к моделям"]
    kc["Поставщик идентификации<br/>OIDC, группы"]
  end

  subgraph inference["Инференс — свой контур"]
    vllm["vLLM на GPU-боксе<br/>MoE-модель в AWQ<br/>tensor-parallel по 4 картам"]
    vl["VL-модель<br/>разбор изображений"]
  end

  subgraph data["Данные"]
    pg[("PostgreSQL<br/>HA-кластер<br/>учёт потребления, биллинг")]
    redis[("Redis<br/>кэш и очереди")]
    qdrant[("Векторная база")]
    s3[("Объектное хранилище<br/>веса моделей, файлы")]
  end

  subgraph obs["Наблюдаемость"]
    lf["Трассировка LLM-трафика"]
    graf["Метрики и дашборды"]
    stat["Статус-страница"]
    an["Веб-аналитика"]
  end

  ext["Агрегатор проприетарных моделей"]

  user --> npm
  npm --> chat & console & docs & drive & academy & mcp & owui
  npm -->|"OpenAI-совместимый API"| gw

  chat -->|"/api/chat"| console
  console -->|"выпуск ключей,<br/>чтение потребления"| gw
  console --> pg
  owui --> gw
  mcp --> gw

  chat & console & drive & academy --> kc

  gw --> vllm
  gw --> vl
  gw -->|"только если ключу разрешено"| ext
  gw --> pg
  gw --> redis

  vllm -.->|"синхронизация весов"| s3
  gw --> lf
  console & gw --> graf
```

Два места, которые стоит заметить:

- **API чата живёт внутри консоли**, а не отдельным сервисом. Причина прямая: чату нужны кошелёк, подписки и та же база — дублировать их означало бы завести вторую правду о деньгах.
- **Векторная база в платформенном слое есть, но продуктового RAG поверх неё нет.** Она обслуживает отдельные сценарии, а не общий поиск по знаниям клиента. Это честное состояние, а не пропуск на схеме.

## C3 — Внутреннее устройство шлюза моделей

```mermaid
flowchart TB
  req["Запрос<br/>Authorization: Bearer &lt;virtual key&gt;"]

  subgraph gw["Шлюз моделей"]
    auth["Проверка ключа<br/>существует · не истёк · не превышен бюджет"]
    perm["<b>Разрешённые модели ключа</b><br/>поле со списком; пусто = видно всё"]
    route["Маршрутизация по имени модели<br/>каталог моделей и цен"]
    ctx["Обогащение контекста<br/>служебные подстановки"]
    call["Вызов upstream<br/>ретраи, таймауты, фолбэк"]
    spend["Запись потребления<br/>токены · стоимость · ключ · модель"]
  end

  subgraph up["Upstream"]
    own["Свой инференс<br/>vLLM"]
    outside["Внешний агрегатор<br/><b>за пределами контура</b>"]
  end

  pg[("PostgreSQL<br/>ключи и потребление")]
  bill["Консоль: счёт клиенту<br/>наценка, BYN"]

  req --> auth --> perm --> route --> ctx --> call
  call --> own
  call -->|"разрешено ключу"| outside
  call --> spend --> pg --> bill

  perm -.->|"не разрешено — отказ<br/>до обращения к upstream"| req
```

**Гейтинг стоит до вызова**, а не после: запрос к внешнему вендору не уходит вовсе, если ключу это не разрешено. Разница принципиальная — при проверке «после» данные уже покинули контур.

Учёт потребления пишется тем же шлюзом, который выполняет запрос. Это даёт биллингу единственный источник правды, но и связывает деньги с доступностью шлюза: **не работает шлюз — не считается потребление**.

## Развёртывание

```mermaid
flowchart TB
  subgraph inet["Интернет"]
    cf["DNS и управление зоной"]
  end

  subgraph dc["Площадка — гипервизор Proxmox"]
    subgraph legacy["Историческая топология: ВМ и контейнеры"]
      npmvm["Обратный прокси"]
      appvms["Продуктовые сервисы<br/><i>переезжают</i>"]
      gwvm["Шлюз моделей<br/><i>оставлен как путь отката</i>"]
    end

    subgraph gpu["GPU-бокс"]
      llx["Инференс-контейнер<br/>vLLM нативно, без docker<br/>4 ускорителя, tensor-parallel"]
    end

    subgraph k8s["Кластер Talos Linux"]
      cp["Плоскость управления"]
      w["Рабочие узлы по группам"]
      ing["Ingress + балансировщик<br/>Cilium как CNI и LB"]
      argo["ArgoCD<br/>GitOps: состояние из репозитория"]
      cm["cert-manager<br/>wildcard через DNS-01"]
      apps["Шлюз моделей, консоль<br/><i>уже здесь</i>"]
    end

    subgraph stateful["Состояние"]
      pgc[("PostgreSQL — HA-кластер")]
      s3c[("Объектное хранилище")]
      vault["Хранилище секретов"]
    end
  end

  cf --> npmvm
  npmvm --> ing
  npmvm --> appvms
  ing --> apps
  apps --> pgc
  apps --> llx
  argo -.->|"синхронизирует"| apps
  llx -.-> s3c
  appvms & apps --> vault
```

Три факта, которые эта схема обязана передать:

1. **Периметр остался на прокси перед кластером.** Публичные имена указывают на него, он терминирует TLS и отправляет трафик либо в кластер, либо на ВМ. Так переезд можно делать по одному сервису, меняя только цель.
2. **Инференс в кластер не переезжает.** Карты физически в боксе, vLLM запущен нативно ради контроля над VRAM. Кластер к нему ходит как к внешнему эндпоинту.
3. **Пути отката живы.** Старый экземпляр шлюза оставлен работающим и смотрит в ту же базу; откат — это смена цели на прокси, а не восстановление из бэкапа. Цена — две копии конфигурации, которые могут разъехаться, и это записано как риск.

## Что на схемах не показано

- **Резервное копирование и аварийное восстановление** — не проверял, утверждать не буду.
- **Сетевая сегментация внутри площадки** — опущена вместе с адресацией.
- **Часть продуктов** (аналитика, статус-страница, служебные сервисы) — показаны собирательно, чтобы не превращать схему в инвентарную ведомость.
