# Диаграммы C4 — как есть

Нарисовано по фактической конфигурации, а не по замыслу. Адреса и идентификаторы узлов — настоящие; полный инвентарь в [отдельном документе](../inventory.md).

## C1 — Контекст

```mermaid
flowchart TB
  subgraph users["Пользователи"]
    org["Клиент-организация<br/>сотрудники, ключи, бюджеты"]
    dev["Разработчик<br/>OpenAI-совместимый ключ"]
    priv["Частный пользователь<br/>кошелёк"]
    staff["Персонал платформы<br/>админ-панель"]
  end

  ZQ["<b>ZubrIQ</b><br/>платформа доступа к языковым моделям<br/>юрисдикция РБ · расчёты в BYN"]

  subgraph external["Внешние системы"]
    agg["OpenRouter<br/>единый upstream-аккаунт,<br/>сотни проприетарных моделей"]
    vendors["Mistral · Alibaba DashScope<br/>прямые маршруты"]
    dns["Cloudflare DNS + Let's Encrypt"]
    play["Google Play<br/>дистрибуция мобильного чата"]
    pay["Приём оплат<br/>счета юрлицам в BYN"]
  end

  org --> ZQ
  dev --> ZQ
  priv --> ZQ
  staff --> ZQ

  ZQ -->|"часть запросов уходит наружу<br/>гейтится правами ключа"| agg
  ZQ --> vendors
  ZQ --- dns
  ZQ --- play
  ZQ --- pay

  ZQ -.->|"свои модели — внутри контура,<br/>наружу не уходят"| ZQ
```

Главное на этом уровне: **часть трафика по замыслу покидает контур**. Это не недосмотр, а второй режим доступа, который продаётся отдельно; вопрос лишь в том, кому он разрешён — см. [безопасность](../security.md).

## C2 — Контейнеры

```mermaid
flowchart TB
  user(["Пользователь / клиентское приложение"])

  subgraph edge["Периметр"]
    npm["Обратный прокси<br/>TLS, маршрутизация по именам<br/>wildcard-сертификат"]
  end

  subgraph products["Продукты"]
    chat["Чат<br/>React + Vercel AI SDK<br/>стриминг SSE"]
    console["Консоль клиента<br/>Laravel + Filament<br/><b>здесь же API чата</b>"]
    docs["Документация"]
    drive["Хранилище документов"]
    academy["Академия"]
    mcp["MCP-шлюз<br/>инструменты для агентов"]
    owui["Открытый чат-интерфейс<br/><i>legacy, до паритета нового</i>"]
  end

  subgraph platform["Платформенный слой"]
    gw["<b>LiteLLM</b><br/>virtual keys · бюджеты · спенд<br/>каталог моделей и цен"]
    kc["Keycloak · realm zubriq<br/>клиенты console/chat/drive<br/>федерация из FreeIPA"]
  end

  subgraph inference["Инференс — свой контур"]
    vllm["vLLM · Qwen3.6-35B-A3B-AWQ<br/>MoE 35B / ~3B активных<br/>TP=4 на Tesla V100"]
    vl["qwen3-vl-4b<br/>разбор изображений"]
  end

  subgraph data["Данные"]
    pg[("Pigsty PostgreSQL · RW-VIP<br/>LiteLLM_SpendLogs, zubriq_console")]
    redis[("Redis")]
    kb[("pgvector в Pigsty · kb_chunks<br/>продуктовый retrieval")]
    s3[("MinIO<br/>веса моделей, файлы")]
  end

  subgraph obs["Наблюдаемость"]
    lf["Langfuse<br/>промпты, стоимость"]
    graf["Grafana"]
    stat["Uptime Kuma"]
    an["Matomo"]
  end

  ext["OpenRouter / Mistral / DashScope"]

  user --> npm
  npm --> chat & console & docs & drive & academy & mcp & owui
  npm -->|"OpenAI-совместимый API"| gw

  chat -->|"/api/chat"| console
  console -->|"выпуск ключей,<br/>чтение потребления"| gw
  console --> pg
  owui --> gw
  mcp --> gw

  chat & console & drive & academy --> kc

  gw --> vllm
  gw --> vl
  gw -->|"только если ключу разрешено"| ext
  gw --> pg
  gw --> redis

  vllm -.->|"синхронизация весов"| s3
  gw --> lf
  console & gw --> graf
```

Два места, которые стоит заметить:

- **API чата живёт внутри консоли**, а не отдельным сервисом. Причина прямая: чату нужны кошелёк, подписки и та же база — дублировать их означало бы завести вторую правду о деньгах.
- **Поиск по знаниям живёт в Postgres, а не в векторной БД.** `kb_chunks` с pgvector и полнотекстом в той же Pigsty; Qdrant остался под Суфлёра и Shield и из продуктового пути выведен пилотом 01.08.2026 — качество определял реранкер, а не хранилище.

## C3 — Внутреннее устройство шлюза моделей

```mermaid
flowchart TB
  req["Запрос<br/>Authorization: Bearer &lt;virtual key&gt;"]

  subgraph gw["Шлюз моделей"]
    auth["Проверка ключа<br/>существует · не истёк · не превышен бюджет"]
    perm["<b>Поле models у ключа</b><br/>список разрешённых; пусто = видно всё"]
    route["Маршрутизация по имени модели<br/>каталог моделей и цен"]
    ctx["Обогащение контекста<br/>служебные подстановки"]
    upcall["Вызов upstream<br/>ретраи, таймауты, фолбэк"]
    spend["LiteLLM_SpendLogs<br/>токены · стоимость · ключ · модель"]
  end

  subgraph up["Upstream"]
    own["Свой инференс<br/>vLLM"]
    outside["OpenRouter и прямые вендоры<br/><b>за пределами контура</b>"]
  end

  pg[("PostgreSQL<br/>ключи и потребление")]
  bill["Консоль: счёт клиенту<br/>наценка, BYN"]

  req --> auth --> perm --> route --> ctx --> upcall
  upcall --> own
  upcall -->|"разрешено ключу"| outside
  upcall --> spend --> pg --> bill

  perm -.->|"не разрешено — отказ<br/>до обращения к upstream"| req
```

**Гейтинг стоит до вызова**, а не после: запрос к внешнему вендору не уходит вовсе, если ключу это не разрешено. Разница принципиальная — при проверке «после» данные уже покинули контур.

Учёт потребления пишется тем же шлюзом, который выполняет запрос. Это даёт биллингу единственный источник правды, но и связывает деньги с доступностью шлюза: **не работает шлюз — не считается потребление**.

## Развёртывание

```mermaid
flowchart TB
  cf["Cloudflare<br/>DNS зон zubriq.by / .ru / .ai"]

  subgraph dc["Площадка — Proxmox pve1…pve4, сеть 10.100.1.0/24"]
    npm["NPM · 10.100.1.12<br/>TLS, wildcard *.zubriq.by (DNS-01)<br/>маршрутизация по именам"]

    subgraph k8s["Кластер Talos · узлы 10.100.1.80–.90"]
      ing["Ingress · LB-пул Cilium 10.100.1.91–.99"]
      argo["ArgoCD<br/>синк из devops/argocd/talos/apps/*"]
      lite["LiteLLM<br/><b>прод-шлюз</b>"]
      cons["console · chat · site"]
      owui["Open WebUI<br/><i>ns openwebui, заморожен</i>"]
      zqc["zubriq-code · mcp-gateway · minio"]
    end

    subgraph gpu["GPU-бокс · LXC 100 «llama» на pve4"]
      vllm["vLLM (systemd, без docker)<br/>4× Tesla V100 SXM2 16 ГБ<br/>Qwen3.6-35B-A3B-AWQ · TP=4<br/>:80 текст · :8001 VL · :8080 пилот"]
    end

    subgraph vms["ВМ и LXC"]
      lite2["LiteLLM legacy · LXC 101<br/>10.100.1.201:4000<br/><i>путь отката, жив</i>"]
      v205["ВМ 205 · 10.100.1.42<br/>стейдж · mcpo · jupyter<br/>Langfuse · code-app"]
      other["Drive .63 · Академия .77 · Dify .55<br/>MCP .58 · Matomo .56 · sandbox .61"]
      kc["Keycloak · 10.100.1.19"]
      ipa["FreeIPA · 10.100.1.15"]
    end

    subgraph state["Состояние"]
      pg[("Pigsty PostgreSQL<br/>.72–.74, RW-VIP 10.100.1.75")]
      minio[("MinIO · .21–.24<br/>бакет models")]
      qd[("Qdrant · 10.100.1.46:6333")]
      vault[("Vault · .52–.54, raft HA")]
    end
  end

  cf --> npm
  npm --> ing
  npm --> other & v205
  ing --> lite & cons & owui & zqc
  lite -->|"свои модели"| vllm
  lite -->|"openrouter/* · mistral · dashscope"| ext(["Внешние вендоры"])
  lite --> pg
  cons --> pg
  cons -->|"выпуск ключей<br/>10.100.1.201:4000"| lite2
  owui -.->|"Service без селектора<br/>EndpointSlice → .42"| v205
  vllm -.->|"mc sync весов"| minio
  kc --> ipa
  cons & owui & other --> kc
  lite & cons & other --> vault
  argo -.-> lite & cons & owui & zqc
```

Пять фактов, которые эта схема обязана передать:

1. **Периметр остался на NPM.** Публичные имена указывают на `10.100.1.12`, он терминирует TLS и отправляет трафик либо в ingress кластера, либо на ВМ. Так переезд делается по одному сервису, меняя только цель.
2. **Инференс в кластер не переезжает.** Карты в боксе, vLLM нативно ради контроля над VRAM. Кластер ходит к нему как к внешнему эндпоинту по `10.100.1.200`.
3. **Путь отката шлюза жив.** LXC 101 работает и смотрит в ту же БД; откат — смена цели на NPM. Консоль, кстати, до сих пор выпускает ключи именно через него — hairpin через `api.zubriq.by` из контура не работает (self-signed).
4. **Кластерный OWUI зависит от ВМ 205.** `mcpo` и `jupyter` остались там, и под ходит на них через Service без селектора с EndpointSlice на `10.100.1.42`: у моделей в БД записаны `tool_ids` вида `http://mcpo:8000/...`, переименовать нельзя. Это делает «переехавший» сервис не до конца переехавшим.
5. **Состояние вынесено.** БД, объектное хранилище, векторная база и секреты живут отдельно от вычислений — поэтому и кластер, и ВМ можно пересоздавать.

## Что на схемах не показано

- **Резервное копирование и аварийное восстановление** — PBS есть, охват и восстановимость не проверял.
- **Сетевая сегментация** — правила Mikrotik (`devops/microtik`) не разбирал; на схемах вся площадка выглядит плоской, так ли это на самом деле — не проверено.
- **Часть сервисов** (Nexus, GitLab, n8n, Wazuh, GlitchTip) — в [инвентаре](../inventory.md), на схемах опущены, чтобы не превращать их в ведомость.
- **Векторная база в C2 показана, но продуктового RAG поверх неё нет** — она обслуживает Суфлёра (ВМ 237) и Shield-сканер, а не общий поиск по знаниям клиента.
